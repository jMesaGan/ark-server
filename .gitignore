# Secret config — never commit real passwords
.env

# Server data and backups (too large for git)
data/
backups/

# Logs
*.log

# arkmanager override (local only)
arkmanager.cfg

# Claude memory
.claude/
